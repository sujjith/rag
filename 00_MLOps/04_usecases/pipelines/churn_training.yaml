# Kubeflow Pipeline: Churn Model Training
# This file is compiled from Python pipeline definition
# Placeholder - will be generated by kfp.compiler
apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  generateName: churn-training-
spec:
  entrypoint: churn-pipeline
  templates:
    - name: churn-pipeline
      steps:
        - - name: fetch-features
            template: fetch-features
        - - name: train-model
            template: train-model
        - - name: evaluate-model
            template: evaluate-model
        - - name: register-model
            template: register-model
