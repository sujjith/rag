apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: airflow-shared-data
  namespace: airflow
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
  storageClassName: local-path
